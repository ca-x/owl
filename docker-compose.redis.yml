services:
  redis:
    image: redislabs/redisearch:2.10.20
    restart: unless-stopped
    command: ["redis-server", "--appendonly", "yes"]
    volumes:
      - owl_redis:/data

  owl:
    depends_on:
      - redis
    environment:
      OWL_REDIS_ADDR: redis:6379

volumes:
  owl_redis:
